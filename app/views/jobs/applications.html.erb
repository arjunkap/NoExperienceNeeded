<div class="container">
<% if @applications %>

	<% @applications.each do |application| %>
	
		<% seeker  = JobSeeker.find(application.job_seeker_id) %>
		
		<div class="row">
	
		<div class="col-md-2"><%= link_to seeker.user.first_name, {controller: "job_seekers", action: "show", id: seeker.id} %></div>
			<div class="col-md-4"><strong><%= Job.find(application.job_id).question3 %></strong> <br>
				<p>	<%= application.question1 %></p>
			</div>

		<div class="col-md-4"></div>

	</div>
	<div class="row">
		<div class="col-md-2"></div>
			<div class="col-md-6"><strong><%= Job.find(application.job_id).question3 %></strong> <br>
				<p>	<%= application.question1 %></p>
			</div>

		<div class="col-md-4"></div>

	</div>
	<div class="row">
		<div class="col-md-2"></div>
			<div class="col-md-6"><strong><%= Job.find(application.job_id).question3 %></strong> <br>
				<p>	<%= application.question1 %></p>
			</div>

		<div class="col-md-4"></div>

	</div>

	<% end %>
	</tbody>
	</table>


<%end%>
</div>